all:
  vars:
    vault_password_file: "/path/to/your/vault_password_file"
  children:
    source:
      hosts:
        sourcesys:
          ansible_host: 192.168.1.100
      vars:
        source_host: "sourcesys"  # Matches the host label above
    destination:
      hosts:
        destsys:
          ansible_host: 192.168.1.200
      vars:
        destination_host: "destsys"  # Matches the host label above
